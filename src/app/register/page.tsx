import { Metadata } from "next";
import Content from "./content";

export const metadata: Metadata = {
  title: "Register | Plant Hub",
};

export default function Register() {
  return <Content />;
}
